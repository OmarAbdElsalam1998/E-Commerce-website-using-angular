/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SortPipe2 } from './sort.pipe';

describe('Pipe: Sorte', () => {
  it('create an instance', () => {
    let pipe = new SortPipe2();
    expect(pipe).toBeTruthy();
  });
});
